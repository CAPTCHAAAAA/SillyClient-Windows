# Bundled Node.js

Run `npm run prepare:runtime` to place Node.js 22.16.0 for Windows x64 in `runtime/node/`.

The downloaded runtime is verified against Node.js `SHASUMS256.txt` and is not committed to Git.
